---
applyTo: "lib/**/*.dart"
---

# Flutter Widget Instructions

- Prefer `StatelessWidget` unless state is required
- Extract reusable widgets into separate files
- Keep `build()` methods focused and readable
- Use `const` widgets where possible for performance
- [Add widget-specific conventions]
